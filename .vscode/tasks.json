{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/api/Fisher.Bookstore.Api.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}